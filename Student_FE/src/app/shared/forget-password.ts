export interface ForgotPasswordViewModel {
    email: string;
  }

  // models/reset-password.model.ts

  export interface ResetPasswordViewModel {
    newPassword: string;
    confirmPassword: string;
  }
  
  
  